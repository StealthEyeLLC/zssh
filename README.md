# zssh

`zssh` is the new repository for the SSH-native persistent Linux computer runtime.

The repository is intentionally initialized as a clean structure first. The audited governing documents are supplied separately as downloadable Markdown files so the project document and naming can be revised before they are committed here.

Start with [`INDEX.md`](INDEX.md).
